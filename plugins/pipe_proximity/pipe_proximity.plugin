# pipe_proximity プラグイン: 可変太さパイプの自己接近検出(pipe_proximity 別 repo・MIT)。
# $PIG_PLUGIN_PATH か ~/.config/srava/plugins/ に置くと pipe_proximity() が使える。
# bin は実際のインストール先に書き換える(テストは CMake が絶対パスで生成)。
op   = pipe_proximity
bin  = /usr/local/libexec/srava/plugins/pipe_proximity_agent
out  = value
args = 1..3
