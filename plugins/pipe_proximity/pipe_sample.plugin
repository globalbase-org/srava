# pipe_sample: 中心線を弧長等間隔ピッチでサンプルし [pos, r] を返す(tube へ直結)。
# pipe_proximity と同一 bin の op。可変太さ管の可視化用(ライブラリの正確な弧長で R(s) 評価)。
# bin は実際のインストール先に書き換える(テストは CMake が絶対パスで生成)。
op   = pipe_sample
bin  = /usr/local/libexec/srava/plugins/pipe_proximity_agent
out  = value
args = 1..3
